# 🚀 Быстрый деплой фронтенда на Netlify

## Проблема: Page not found (404) на https://shortsai.ru

Это означает, что фронтенд не задеплоен или деплой не завершился успешно.

## ✅ Решение: Деплой через Netlify Dashboard

### Шаг 1: Подключите репозиторий к Netlify

1. Откройте [Netlify Dashboard](https://app.netlify.com)
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и репозиторий `hotwellkz/p046`
4. Настройте Build settings:
   - **Base directory:** (оставьте пустым)
   - **Build command:** `npm run build`
   - **Publish directory:** `dist`

### Шаг 2: Установите переменные окружения

В Netlify Dashboard → **Site settings** → **Environment variables** добавьте:

```
VITE_FIREBASE_API_KEY=AIzaSyCtAg...s0sQ
VITE_FIREBASE_AUTH_DOMAIN=prompt-6a4fd.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=prompt-6a4fd
VITE_FIREBASE_STORAGE_BUCKET=prompt-6a4fd.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=905027425668
VITE_FIREBASE_APP_ID=1:905027425668:web:38f58912370df2c2be39d1
VITE_API_BASE_URL=https://shortsai-backend-905027425668.us-central1.run.app
```

**Важно:** Все переменные для клиентской части должны начинаться с `VITE_`!

### Шаг 3: Запустите деплой

1. После настройки Netlify автоматически запустит деплой
2. Или нажмите **"Trigger deploy"** → **"Deploy site"**

### Шаг 4: Проверьте результат

1. Откройте https://shortsai.ru
2. Проверьте консоль браузера (F12):
   - Должно быть: `🔥 Firebase конфигурация: {...}`
   - Должно быть: `✅ Firebase успешно инициализирован`
   - Должно быть: `[API Config] Using API base URL: https://shortsai-backend-905027425668.us-central1.run.app`

## 🔧 Альтернатива: Деплой через Netlify CLI

Если у вас установлен Netlify CLI:

```powershell
# 1. Войдите в Netlify
netlify login

# 2. Инициализируйте сайт (если первый раз)
netlify init

# 3. Задеплойте (Netlify соберёт проект автоматически)
netlify deploy --prod
```

## ⚙️ Настройки Netlify (уже настроены в netlify.toml)

- **Build command:** `npm run build`
- **Publish directory:** `dist`
- **Redirects:** Настроены для SPA (все запросы → `/index.html`)

## ✅ После деплоя проверьте

1. ✅ Главная страница открывается
2. ✅ Нет ошибок 404 при переходе по ссылкам
3. ✅ Обновление страницы (F5) работает
4. ✅ Firebase инициализирован
5. ✅ API запросы идут на правильный backend URL

## 📝 Если деплой не работает

1. Проверьте логи деплоя в Netlify Dashboard
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что `netlify.toml` находится в корне репозитория
4. Убедитесь, что `public/_redirects` содержит: `/*    /index.html   200`

