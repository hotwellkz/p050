# 🚀 Инструкция по деплою фронтенда на Netlify

## Проблема: Page not found (404)

Если при открытии `https://shortsai.ru` вы видите ошибку "Page not found", это означает, что:
1. Фронтенд не задеплоен на Netlify
2. Или деплой не завершился успешно

## Решение: Деплой через Netlify CLI

### Вариант 1: Автоматический деплой через Git (рекомендуется)

1. **Подключите репозиторий к Netlify:**
   - Откройте [Netlify Dashboard](https://app.netlify.com)
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите GitHub и репозиторий `hotwellkz/p046`
   - Настройте:
     - **Build command:** `npm run build`
     - **Publish directory:** `dist`
     - **Base directory:** (оставьте пустым, если фронтенд в корне)

2. **Установите переменные окружения:**
   - Site settings → Environment variables
   - Добавьте все переменные из `NETLIFY_ENV_VARS.md`

3. **Запустите деплой:**
   - Netlify автоматически задеплоит при push в main/master

### Вариант 2: Ручной деплой через Netlify CLI

```powershell
# 1. Установите Netlify CLI (если ещё не установлен)
npm install -g netlify-cli

# 2. Соберите фронтенд
npx vite build --mode production

# 3. Войдите в Netlify
netlify login

# 4. Инициализируйте сайт (если первый раз)
netlify init

# 5. Задеплойте
netlify deploy --prod
```

### Вариант 3: Деплой через Netlify Dashboard (Drag & Drop)

1. **Соберите фронтенд локально:**
   ```powershell
   npx vite build --mode production
   ```

2. **Загрузите папку `dist` в Netlify:**
   - Откройте [Netlify Dashboard](https://app.netlify.com)
   - Перетащите папку `dist` в область "Deploy manually"

## ⚙️ Настройки Netlify

### Build settings (Site settings → Build & deploy → Build settings)

- **Build command:** `npm run build`
- **Publish directory:** `dist`
- **Base directory:** (пусто, если фронтенд в корне)

### Environment variables (Site settings → Environment variables)

Добавьте все переменные из `NETLIFY_ENV_VARS.md`:

```
VITE_FIREBASE_API_KEY=AIzaSy...
VITE_FIREBASE_AUTH_DOMAIN=prompt-6a4fd.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=prompt-6a4fd
VITE_FIREBASE_STORAGE_BUCKET=prompt-6a4fd.firebasestorage.app
VITE_FIREBASE_MESSAGING_SENDER_ID=905027425668
VITE_FIREBASE_APP_ID=1:905027425668:web:38f58912370df2c2be39d1
VITE_API_BASE_URL=https://shortsai-backend-905027425668.us-central1.run.app
```

### Redirects (уже настроено в `netlify.toml`)

Файл `netlify.toml` уже содержит правильные настройки для SPA:
```toml
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
```

Также файл `public/_redirects` автоматически копируется в `dist/`.

## ✅ Проверка после деплоя

1. **Откройте сайт:** https://shortsai.ru
2. **Проверьте консоль браузера (F12):**
   - Должно быть: `🔥 Firebase конфигурация: {...}`
   - Должно быть: `✅ Firebase успешно инициализирован`
   - Должно быть: `[API Config] Using API base URL: https://shortsai-backend-905027425668.us-central1.run.app`
3. **Проверьте Network tab:**
   - Запросы к API должны идти на правильный backend URL
   - Не должно быть CORS ошибок
4. **Проверьте роутинг:**
   - Переход по прямой ссылке (например, `/channels`) должен работать
   - Обновление страницы (F5) не должно вызывать 404

## 🔧 Troubleshooting

### Ошибка 404 на всех страницах

**Причина:** Неправильные redirects или отсутствие `_redirects` файла.

**Решение:**
1. Убедитесь, что `public/_redirects` содержит: `/*    /index.html   200`
2. Убедитесь, что `netlify.toml` содержит правильные redirects
3. Пересоберите и задеплойте

### Ошибка "Firebase not initialized"

**Причина:** Не установлены переменные окружения Firebase.

**Решение:**
1. Проверьте переменные окружения в Netlify Dashboard
2. Убедитесь, что все переменные начинаются с `VITE_`
3. Пересоберите проект

### CORS ошибки

**Причина:** Backend не настроен для работы с фронтендом.

**Решение:**
1. Убедитесь, что `FRONTEND_ORIGIN=https://shortsai.ru` установлен на Cloud Run
2. Проверьте CORS настройки в `backend/src/index.ts`

## 📝 Команды для быстрого деплоя

```powershell
# Сборка
npx vite build --mode production

# Деплой через CLI
netlify deploy --prod

# Или через Git (автоматически при push)
git push origin main
```

